{
    "version": 2,
    "build": [
        {
            "src": "api/*.js",
            "use": "@versel/node"
        }
    ],
    "routes": [
        {
            "src": "/api/jobs",
            "dest": "/api/jobs.js"
        },
        {
            "src": "/api/jobs/:id",
            "dest": "/api/job.js"
        }
    ]
}